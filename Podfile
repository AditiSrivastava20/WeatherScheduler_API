# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'scheduletask' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for scheduletask

  target 'scheduletaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'scheduletaskUITests' do
    inherit! :search_paths
    # Pods for testing
  end
pod ‘ObjectMapper’
pod ‘Alamofire’
pod ‘SVProgressHUD’
end
